<aura:component controller="OutletManager">

    <aura:handler name="init" value="{!this}" action="{!c.doInit}" />
    <aura:attribute name="mapMarkers" type="Object[]" />

    <lightning:map mapMarkers="{! v.mapMarkers }" markersTitle="{!$Label.c.OutOutlets}" />

</aura:component>